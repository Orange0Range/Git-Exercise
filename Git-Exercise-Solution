mkdir learn_git_again					Create a folder called learn_git_again.
cd learn_git_again						cd into the learn_git_again folder.
touch third.txt							Create a file called third.txt.
git init								Initialize an empty git repository.
git add third.txt						Add third.txt to the staging area.
git commit -m 'adding third.txt'		Commit with the message “adding third.txt”.
git log									Check out your commit with git log.
touch fourth.txt						Create another file called fourth.txt.
git add fourth.txt						Add fourth.txt to the staging area.
git commit -m 'adding fourth.txt'		Commit with the message “adding fourth.txt”
rm third.txt							Remove the third.txt file
git add third.txt						Add this change to the staging area
git commit -m 'removing third.txt'		Commit with the message “removing third.txt”
git log									Check out your commits using git log
git config --global core.pager 'cat'	Change your global setting to core.pager=cat - you can read more about that [here](https://git-scm.com/book/en/v2/Customizing-Git-Git-Configuration).
git config --global -l					Write the command to list all of the global configurations for git on your machine. You can type git config --global to find out how to do this
